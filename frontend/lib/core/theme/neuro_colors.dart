import 'package:flutter/material.dart';

class NeuroColors {
  static const Color primary = Color(0xFF0056D2); // Deep Blue for active states
  static const Color secondary = Color(0xFF10B981); // Green for stable/success
  static const Color accent = Color(0xFF00E5FF); // Cyan for glowing effects
  static const Color emergency = Color(0xFFFF2A2A); // Bright Red for critical/alerts
  
  static const Color background = Color(0xFF060B19); // Very dark navy blue background
  static const Color surface = Color(0xFF0D1427); // Slightly lighter navy for cards
  static const Color surfaceHighlight = Color(0xFF17203A); // For inner cards
  
  static const Color textPrimary = Color(0xFFFFFFFF); 
  static const Color textSecondary = Color(0xFF94A3B8);
  
  static const Color border = Color(0xFF1F2937); 
  
  static const Color brainRiskHigh = Color(0xFFFF2A2A);
  static const Color brainRiskMedium = Color(0xFFF59E0B);
  static const Color brainRiskLow = Color(0xFF10B981);
}
